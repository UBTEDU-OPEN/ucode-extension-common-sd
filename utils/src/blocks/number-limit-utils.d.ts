export declare function NumberLimit(options: {
    max: number;
    min: number;
    value: string | number;
    fixed?: number;
}): number;
export declare function isInvalid(number: number, min: number, max: number): boolean;
